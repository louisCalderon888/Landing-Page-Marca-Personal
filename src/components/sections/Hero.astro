---
import Button from '../ui/Button.astro';
---

<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Enhanced Background with Tech Grid -->
  <div class="absolute inset-0 tech-grid-pattern opacity-60 pointer-events-none"></div>
  <div class="absolute inset-0 bg-gradient-to-b from-dark-950 via-dark-950/95 to-dark-950 pointer-events-none"></div>
  
  <!-- Subtle Aurora Effect -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute -top-1/2 -left-1/4 w-full h-full bg-gradient-radial from-brand-blue/10 via-transparent to-transparent blur-3xl animate-pulse-slow"></div>
    <div class="absolute -bottom-1/2 -right-1/4 w-full h-full bg-gradient-radial from-brand-green/5 via-transparent to-transparent blur-3xl animate-pulse-slow" style="animation-delay: 1s;"></div>
  </div>
  
  <!-- Content -->
  <div class="relative container-custom py-24 md:py-32 z-10">
    <div class="max-w-5xl mx-auto text-center">
      <!-- Badge with Premium Glow + Authority Bias -->
      <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-brand-green/10 border border-brand-green/30 text-brand-green text-sm font-medium mb-8 animate-fade-in badge-glow">
        <span class="pulse-dot"></span>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M6.267 3.455a3.066 3.066 0 001.745-.723 3.066 3.066 0 013.976 0 3.066 3.066 0 001.745.723 3.066 3.066 0 012.812 2.812c.051.643.304 1.254.723 1.745a3.066 3.066 0 010 3.976 3.066 3.066 0 00-.723 1.745 3.066 3.066 0 01-2.812 2.812 3.066 3.066 0 00-1.745.723 3.066 3.066 0 01-3.976 0 3.066 3.066 0 00-1.745-.723 3.066 3.066 0 01-2.812-2.812 3.066 3.066 0 00-.723-1.745 3.066 3.066 0 010-3.976 3.066 3.066 0 00.723-1.745 3.066 3.066 0 012.812-2.812zm7.44 5.252a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
        </svg>
        Track Record Verificado Científicamente
      </div>
      
      <!-- Headline with Loss Aversion + Authority -->
      <h1 class="font-display font-extrabold text-5xl md:text-6xl lg:text-7xl mb-6 text-shadow animate-fade-in">
        <span class="text-brand-amber">Deja de Perder Dinero</span>
        <span class="block mt-2 bg-gradient-to-r from-brand-blue via-brand-green to-brand-blue bg-clip-text text-transparent bg-[length:200%_auto] animate-gradient">
          Operando con Emociones
        </span>
      </h1>
      
      <!-- Subheadline with Present Bias (immediate benefit) -->
      <p class="text-xl md:text-2xl text-dark-300 mb-12 max-w-3xl mx-auto leading-relaxed animate-fade-in">
        <span class="text-white font-semibold">Hoy mismo</span> puedes acceder a sistemas algorítmicos con 
        <span class="text-brand-green">94% ROI verificado</span> y transparencia total. 
        <span class="text-dark-400">Para traders serios únicamente.</span>
      </p>

      
      <!-- Stats Row - Premium Glass Cards -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12 animate-fade-in">
        <!-- ROI Card -->
        <div class="premium-card p-6 group">
          <div class="relative z-10">
            <div class="metric-number-glow text-brand-green mb-2 group-hover:scale-105 transition-transform duration-300">94%</div>
            <div class="text-dark-400 text-sm uppercase tracking-wider font-medium">ROI en 16 meses</div>
          </div>
        </div>
        
        <!-- Drawdown Card -->
        <div class="premium-card p-6 group">
          <div class="relative z-10">
            <div class="metric-number text-brand-amber mb-2 group-hover:scale-105 transition-transform duration-300">-48%</div>
            <div class="text-dark-400 text-sm uppercase tracking-wider font-medium">Drawdown Máximo</div>
            <div class="text-xs text-dark-500 mt-2 opacity-0 group-hover:opacity-100 transition-opacity duration-300">Transparencia total</div>
          </div>
        </div>
        
        <!-- Algorithmic Card -->
        <div class="premium-card p-6 group">
          <div class="relative z-10">
            <div class="metric-number-glow text-brand-blue mb-2 group-hover:scale-105 transition-transform duration-300">100%</div>
            <div class="text-dark-400 text-sm uppercase tracking-wider font-medium">Algorítmico</div>
            <div class="text-xs text-dark-500 mt-2 opacity-0 group-hover:opacity-100 transition-opacity duration-300">Cero emociones</div>
          </div>
        </div>
      </div>
      
      <!-- CTAs with Premium Glow Effects -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center animate-fade-in">
        <Button variant="cta" size="lg" href="#track-record" class="btn-glow btn-glow-cta">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM9.555 7.168A1 1 0 008 8v4a1 1 0 001.555.832l3-2a1 1 0 000-1.664l-3-2z" clip-rule="evenodd" />
          </svg>
          Ver Track Record
        </Button>
        <Button variant="secondary" size="lg" href="#services" class="btn-glow">
          Conocer Servicios
        </Button>
      </div>
      
      <!-- Signature Phrase with Typing Effect -->
      <p class="mt-12 text-dark-400 text-sm font-mono animate-fade-in typing-cursor">
        No opino, valido con datos
      </p>
    </div>
  </div>
  
  <!-- Scroll Indicator with Glow -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
    <div class="p-2 rounded-full bg-dark-900/50 backdrop-blur-sm border border-dark-700/50">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-brand-blue" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </div>
  </div>
</section>

<style>
  @keyframes pulse-slow {
    0%, 100% {
      opacity: 0.3;
      transform: scale(1);
    }
    50% {
      opacity: 0.5;
      transform: scale(1.05);
    }
  }
  
  .animate-pulse-slow {
    animation: pulse-slow 8s ease-in-out infinite;
  }
</style>
